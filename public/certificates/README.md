# Certificate Assets Directory

Place your certificate images or files here. Make sure their filenames match the `id` field defined in `data/certificates.ts`!

For example:
- `aws.png` (or `.jpg`, `.pdf` - just update `imagePath` in `data/certificates.ts` if you change the file extension)
- `meta.png`
- `mongodb.png`
